Version 50: the archive habit becomes a skill you can carry anywhere.

The skill is git-ai-instruct-injection, as you named it, and it is attached to this message: press Save skill on the card and it joins your profile beside release-workflow and git-identity, so it applies in every project. It keeps the same habit this repository has kept since version 1 — the folder prompt text/N with the prompt that produced the version exactly as written, the reply that shipped it, the three-line model attribution and every file you supplied, swapped in wholesale with each release so a repository carries only the version in service — and it now does the mechanical part for you. Four small scripts: one starts a version, working out the number from the folder in service and clearing the old one; one writes the prompt or the reply from whatever it is given, dropping only the newline a here-document adds, and refusing an empty record; one copies your files in under their own names, without the upload's hash prefix; and one checks the archive is whole before it ships — one folder, three sound records, a model file of exactly three lines, everything staged — and says plainly what is wrong when it is not. A new project starts at version 1 and gets the policy written into its CLAUDE.md, so later sessions keep it without being told.

This repository now carries the same skill in place of the old prompt-archive one, and this release was archived with it: the three messages that asked for it, in order, as version 50.

Verified in a scratch repository: a version started, the prompt written from a here-document with its two messages a blank line apart and no stray newline, two files attached with the hash prefix gone and a saved picture kept as it was, the check refusing until the reply was written and everything staged and passing once it was, the next version started by number alone with the old folder removed, the earlier record still readable from the release that shipped it, and the refusals — an empty text, a wrong kind, a version not yet started, two folders in service, a model file of the wrong length — each with its reason. The package validates as a skill, and the site is unchanged: the deploy dry run is clean and the page renders as before at full width and at 500 px.