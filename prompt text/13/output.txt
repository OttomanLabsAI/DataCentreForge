Noted on the naming — flipping the labels to Clearance this round. The rest is a proper feature set: conduit arrays (rows × cols per run), a per-type array spacing where the face grid snaps to the biggest one present, a chamber edge-clearance setting, and a face section view with stacking controls when you select a manhole. Let me read the pieces I'm about to touch:

Ran 14 commands, read a file · 2 notes

Labels flipped back to Clearance everywhere — my bad on the direction.

The rest is in, all of it. Select a manhole and the panel now grows a Faces with connections section: each face that carries runs gets a list row per run (type, array size, level tag) and a to-scale 2D section view of that face — wall band, every conduit drawn as a circle in its spec colour, dashed edge-clearance guides, and a caption reading out pitch, edge clearance, and whether the grid fits the face width.

Arrays: every run now has Columns (wide) and Rows (high) in the run panel. In plan, each column draws as its own parallel duct following the routed centreline; rows sit below each other so they collapse in plan but show in the section. Routing knows the array's true width — the outermost conduit gets the bend radius, straight lengths and clearances budgeted for it, so a 4-wide array swings wider around an obstacle than a single duct would.

Stacking: the ▲▼ buttons next to each run in the face list move it up or down (it's the run's level — L0 is highest). Two arrays side by side too wide for the face? Drop one a level and they stack on the same grid instead — the section redraws live and the width check follows.

Alignment: each conduit spec now has an Array spacing (MV 600, LV 450, ELV/FIBRE/TELECOMMS 300 seeded, editable). A face uses one shared grid at the biggest spacing among the types present — so if ELV at 300 shares a face with MV at 600, everything sits on 600 centres and the columns line up across arrays, exactly as you described. The manhole's lateral spacing acts as the floor. And the new Edge clearance setting on the manhole is the minimum from the face edge to the nearest conduit's edge — it feeds the "too narrow" warning and shows as the dashed guides in the section.

One thing to know: level changes and array sizes re-route live, and a face that can't carry its grid flags every run on it amber rather than silently squeezing.
